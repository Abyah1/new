<div class="product-card">
  <img src="{{ product.image }}" alt="{{ product.name }}">
  <h3>{{ product.name }}</h3>
  <p>{{ product.price }} ريال</p>
  <a href="/product/{{ product.id }}">عرض التفاصيل</a>
</div>
